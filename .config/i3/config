# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $mod3 Mod3

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Noto Sans Mono 12

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex-autostart --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xset dpms 0 0 0
exec --no-startup-id xset s 0
exec --no-startup-id xss-lock --transfer-sleep-lock -- ~/.config/lock.sh

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet
exec --no-startup-id xrandr --output eDP --mode 1920x1080 --rate 60
exec --no-startup-id xrandr --output HDMI-1-0 --mode 1920x1080 --rate 100 --right-of eDP

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec terminator
bindsym $mod+mod2+KP_Enter exec terminator

# kill focused window
bindsym $mod+w kill

# start dmenu (a program launcher)
#bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
bindsym $mod+p exec --no-startup-id /home/nima/.config/rofi/scripts/launcher_t1
bindsym $mod+l exec --no-startup-id /home/nima/.config/rofi/scripts/powermenu_t1
bindsym $mod+t exec --no-startup-id /home/nima/.config/rofi/scripts/window

# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus
#bindsym $mod+j focus left
#bindsym $mod+k focus down
#bindsym $mod+l focus up
#bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
bindsym $mod+s layout toggle tabbed split
bindsym $mod+x layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+j floating toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+mod2+KP_1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+mod2+KP_2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+mod2+KP_3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+mod2+KP_4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+mod2+KP_5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+mod2+KP_6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+mod2+KP_7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+mod2+KP_8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+mod2+KP_9 workspace number $ws9
bindsym $mod+0 workspace number $ws10
bindsym $mod+mod2+KP_0 workspace number $ws10

#workspace 1 output eDP
#workspace 2 output HDMI-1-0 
#workspace 7 output HDMI-1-0
#workspace 8 output HDMI-1-0
#workspace 9 output HDMI-1-0

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+KP_End move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+KP_Down move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+KP_Next move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+KP_Left move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+KP_Begin move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+KP_Right move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+KP_Home move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+KP_Up move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+KP_Prior move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10
bindsym $mod+Shift+KP_Insert move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

#bar {
#  position top
  #status_command i3blocks
#  font pango:Noto Sans Mono 12
#}

exec_always --no-startup-id $HOME/.config/polybar/launch.sh


#bar {
#        status_command i3status --config /home/nima/.config/i3/i3status.conf
#        position top
#        workspace_buttons yes
#        font pango:Noto Sans Mono 12
#
#        colors {
#            background #000000
#            statusline #ffffff
#            focused_workspace  #ffffff #285577
#            active_workspace   #ffffff #333333
#            inactive_workspace #888888 #222222
#            urgent_workspace   #ffffff #900000
#        }
#}

# My configs
bindsym $mod+d move workspace to output right
bindsym $mod+Prior workspace prev_on_output
bindsym $mod+KP_Prior workspace prev_on_output
bindsym $mod+Next workspace next_on_output
bindsym $mod+KP_Next workspace next_on_output

bindsym $mod+Control+Left resize shrink width 2 px or 2 ppt
bindsym $mod+Control+Down resize grow height 2 px or 2 ppt
bindsym $mod+Control+Up resize shrink height 2 px or 2 ppt
bindsym $mod+Control+Right resize grow width 2 px or 2 ppt


for_window [window_role="About"] floating enable
for_window [window_role="Organizer"] floating enable
for_window [window_role="Preferences"] floating enable
for_window [window_role="bubble"] floating enable
for_window [window_role="page-info"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_role="toolbox"] floating enable
for_window [window_role="webconsole"] floating enable
for_window [window_type="dialog"] floating enable
for_window [window_type="menu"] floating enable
for_window [class="Thunar"] resize set 640 px 480 px 
for_window [window_role="(?i)GtkFileChooserDialog"] resize set 640 px 480 px 
#for_window [class=".*"] resize set 640 px 480 px

#for_window [class="Slack"] border normal
#for_window [class="bombsquad"] border normal
for_window [all] title_window_icon on
for_window [all] title_window_icon padding 5px

#default_border pixel 0
#default_floating_border pixel 0

#floating_minimum_size 200 x 200
#floating_maximum_size 1500 x 1500

# class                 border  backgr. text    indicator child_border
#client.focused          #61759c #61759c #ffffff #61759c #285577
client.focused          #5294e2 #5294e2 #ffffff #5294e2   #285577
client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
client.unfocused        #333333 #222222 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c

client.background       #ffffff

#exec_always --no-startup-id feh --bg-scale /home/nima/Pictures/F40-01-day-compressed.png
exec_always --no-startup-id nitrogen --restore
exec_always --no-startup-id i3-autolayout autolayout
exec_always --no-startup-id /home/nima/.config/greenclip daemon>/dev/null
exec_always "setxkbmap -model pc104 -layout us,ir -variant ,, -option grp:win_space_toggle"
#exec --no-startup-id betterlockscreen -l
exec_always --no-startup-id /usr/bin/gnome-keyring-daemon --start --components=secrets
exec_always --no-startup-id /usr/libexec/polkit-gnome-authentication-agent-1 >/dev/null 2>&1

bindsym $mod+y exec --no-startup-id terminator -e /home/nima/scripts/yazi-run.sh>/dev/null 2>/dev/null
bindsym $mod+k exec --no-startup-id nautilus --no-desktop>/dev/null 2>/dev/null
bindsym Shift+Print exec --no-startup-id /home/nima/.config/rofi/applets/bin/screenshot.sh >/dev/null 2>/dev/null
bindsym $mod+q exec --no-startup-id rofi -theme "$HOME/.config/rofi/applets/type-1/style-2.rasi"  -show rofi-sound -modi "rofi-sound:/home/nima/.config/rofi-sound/rofi-sound-output-chooser"
bindsym $mod+c exec --no-startup-id rofi -theme "$HOME/.config/rofi/launchers/type-1/style-2.rasi" -modi "clipboard:/home/nima/.config/greenclip print" -show clipboard -run-command '{cmd}' 
#bindsym $mod+c exec --no-startup-id rofi -theme "$HOME/.config/rofi/applets/type-1/style-1.rasi" -modi "clipboard:/home/nima/.config/greenclip print" -show clipboard -run-command '{cmd}' && sleep 0.25 && xdotool type $(xclip -o -selection clipboard)
bindsym $mod+BackSpace exec --no-startup-id "/home/nima/.config/greenclip clear"

exec_always --no-startup-id picom --config ~/.config/picom.conf -b
exec_always --no-startup-id blueman-applet>/dev/null 2>&1
exec --no-startup-id xinput set-prop "ASUE120A:00 04F3:319B Touchpad" "libinput Natural Scrolling Enabled" 1
exec --no-startup-id xinput set-prop "ASUE120A:00 04F3:319B Touchpad" "libinput Tapping Enabled" 1
exec --no-startup-id setxkbmap -option caps:hyper

#exec_always --no-startup-id xfsettingsd

gaps inner 7
gaps outer 0

workspace_layout tabbed

